{
  "name": "registry-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 8080 --routes routes.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "gdn.anthony@gmail.com",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.15.1"
  }
}
